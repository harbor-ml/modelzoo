apiVersion: v1
kind: Service
metadata:
  name: js-server-serv
spec:
  clusterIP: None
  ports:
  - name: http
    port: 5000
    targetPort: http
    protocol: TCP
  selector:
    container: js-container
