apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: example
spec:
  rules:
  - host: localhost
    http:
      paths:
      - path: /
        backend:
          serviceName: js-mockserver-serv
          servicePort: 80
